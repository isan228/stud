<%- include('partials/header', { title: 'Статистика' }) %>

<div class="row">
    <div class="col-12">
        <div class="card shadow">
            <div class="card-header bg-primary text-white">
                <h3 class="card-title mb-0">Статистика</h3>
            </div>
            <div class="card-body">
                <p>В этом разделе вы увидите тесты которые проходили за всё время.</p>

                <% if (typeof results !== 'undefined' && results.length > 0) { %>
                    <div class="table-responsive">
                        <table class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>Дата/Время</th>
                                    <th>Балл</th>
                                    <th>Название теста</th>
                                    <th>Тип</th>
                                    <th>Предмет</th>
                                    <th>Количество вопросов</th>
                                    <th>Действия</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% results.forEach(result => { %>
                                    <tr>
                                        <td><%= new Date(result.completedAt).toLocaleString('ru-RU') %></td>
                                        <td><strong><%= result.score %>/<%= result.totalQuestions %></strong></td>
                                        <td><%= result.Test.title %></td>
                                        <td><%= result.Test.type %></td>
                                        <td><%= result.Test.subject %></td>
                                        <td><%= result.totalQuestions %></td>
                                        <td>
                                            <a href="/tests/statistics/<%= result.id %>" class="btn btn-sm btn-primary">Просмотр</a>
                                        </td>
                                    </tr>
                                <% }); %>
                            </tbody>
                        </table>
                    </div>
                <% } else { %>
                    <div class="alert alert-info">
                        Вы еще не проходили тесты. Начните с <a href="/tests/emulator">эмулятора</a>.
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</div>

<%- include('partials/footer') %>





